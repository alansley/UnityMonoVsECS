using Unity.Entities;

/// <summary>
/// Just a tag to add to our particle entities so we can easily find the particles via a query.
/// </summary>
public struct ECSParticleComponentTag : IComponentData { }